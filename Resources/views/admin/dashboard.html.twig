{% extends '@IServCore/page.html.twig' %}

{% block content %}
    {# 1. Die Navigation holen (aus der ausgelagerten Datei) #}
    {% include '@PulsRSportabzeichen/admin/_tabs.html.twig' with {'activeTab': activeTab} %}

    {# 2. Der INHALT, der nur auf der Startseite zu sehen ist #}
    <div class="panel panel-default">
        <div class="panel-body">
            <h3>Herzlich Willkommen in der Verwaltung</h3>
            <p>Wählen Sie oben einen Reiter aus...</p>
        </div>
    </div>
{% endblock %}