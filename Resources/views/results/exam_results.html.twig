{% extends '@IServCore/page.html.twig' %}

{% block title %}
    Ergebnisse eingeben – {{ exam.exam_name }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/sportabzeichen_results.css', 'pulsr-sportabzeichen') }}">
{% endblock %}

{% block content %}
<div class="container-fluid mt-3">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h4>🏅 {{ exam.exam_name }} ({{ exam.exam_year }})</h4>
        <a href="{{ path('sportabzeichen_results_exams') }}" class="btn btn-outline-secondary btn-sm">Zurück</a>
    </div>

    <form id="autosave-form" 
          data-global-route="{{ path('sportabzeichen_results_exam_result_save') }}"
          data-global-token="{{ csrf_token('sportabzeichen_result_save') }}">

        <div class="table-responsive">
            <table class="table table-bordered align-middle table-results-compact bg-white">
                <thead class="table-light text-center">
                    <tr>
                        <th class="col-name text-start">Teilnehmer (Alter)</th>
                        <th class="col-class">Klasse</th>
                        {% for category, items in disciplines %}
                            {% if category|trim|upper != 'SWIMMING' and category|trim|upper != 'SCHWIMMEN' %}
                                <th class="col-discipline">{{ category }}</th>
                            {% endif %}
                        {% endfor %}
                    </tr>
                </thead>

                <tbody>
                {% for p in participants %}
                    {# Berechnungsteil (totalPoints, hasSwimmingProof) bleibt wie in der Vorherigen Nachricht #}
                    {% set epResults = results[p.ep_id] ?? {} %}
                    {% set totalPoints = 0 %}
                    {% set hasSwimmingProof = false %}
                    {% for dId, resData in epResults %}
                        {% if resData.kategorie|default('')|trim|upper != 'SWIMMING' and resData.kategorie|default('')|trim|upper != 'SCHWIMMEN' %}
                            {% set totalPoints = totalPoints + (resData.points|default(0)) %}
                        {% endif %}
                        {% if resData.schwimmnachweis|default(false) and resData.points|default(0) > 0 %}
                            {% set hasSwimmingProof = true %}
                        {% endif %}
                    {% endfor %}

                    <tr data-ep-id="{{ p.ep_id }}" data-birth-year="{{ exam.exam_year - p.age_year }}">
                        <td class="col-name name-container">
                            <div class="fw-bold">
                                {{ p.vorname }} {{ p.nachname }} 
                                <span class="text-muted" style="font-weight: normal;">({{ p.age_year }})</span>
                            </div>
                            <div class="mt-1 d-flex gap-1">
                                <span class="badge {{ hasSwimmingProof ? 'bg-success' : 'bg-danger' }}">
                                    {{ hasSwimmingProof ? '🏊 OK' : '❌ Nachweis' }}
                                </span>
                                <span class="badge bg-info text-dark" id="total-points-{{ p.ep_id }}">
                                    {{ totalPoints }} Pkt.
                                </span>
                            </div>
                        </td>

                        {# KLASSE: Probiert verschiedene Varianten aus dem Controller #}
                        <td class="col-class text-center align-middle fw-bold">
                            {{ p.class_name|default(p.class)|default(p.group)|default(p.klasse)|default('-') }}
                        </td>

                        {# DISZIPLINEN #}
                        {% for category, items in disciplines %}
                            {% if category|trim|upper != 'SWIMMING' and category|trim|upper != 'SCHWIMMEN' %}
                                {# Logik für options und selectedId wie bisher #}
                                {# ... #}
                                <td class="p-1 col-discipline">
                                    <select class="form-select discipline-selector mb-1 {{ mClass }}" ...>
                                        {# ... options ... #}
                                    </select>
                                    <input type="text" class="form-control form-control-custom {{ mClass }}" ...>
                                    <div class="req-hint">
                                        {# ... Bronze/Silber/Gold Werte ... #}
                                    </div>
                                </td>
                            {% endif %}
                        {% endfor %}
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </form>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/exam_results_autosave.js', 'pulsr-sportabzeichen') }}"></script>
{% endblock %}