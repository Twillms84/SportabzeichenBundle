{% extends '@IServCore/page.html.twig' %}

{% block title %}
    Ergebnisse eingeben – {{ exam.exam_name }} ({{ exam.exam_year }})
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/sportabzeichen_results.css', 'pulsr-sportabzeichen') }}">
{% endblock %}

{% block content %}
<div class="container-fluid mt-3">
    <h2 class="mb-3">🏅 Ergebnisse: {{ exam.exam_name }}</h2>

    {# Filter #}
    <div class="card mb-3 shadow-sm">
        <div class="card-body py-2">
            <form method="get" class="row g-2 align-items-center">
                <div class="col-auto"><label class="small fw-bold">Klasse:</label></div>
                <div class="col-auto">
                    <select name="class" class="form-select form-select-sm w-auto">
                        <option value="">(Alle)</option>
                        {% for c in classes %}
                            <option value="{{ c.klasse }}" {% if selectedClass == c.klasse %}selected{% endif %}>{{ c.klasse }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="col-auto">
                    <button type="submit" class="btn btn-sm btn-primary">Filtern</button>
                    <a href="{{ path('sportabzeichen_results_index', {examId: exam.id}) }}" class="btn btn-sm btn-outline-secondary">Reset</a>
                </div>
            </form>
        </div>
    </div>

    {# Das Formular muss diese IDs und Data-Attribute haben für das JS #}
    <form id="autosave-form" 
        data-global-route="{{ path('sportabzeichen_results_exam_result_save') }}" 
        data-global-token="{{ csrf_token('sportabzeichen_result_save') }}"
        data-exam-id="{{ exam.id }}"> {# Wichtig für den Controller! #}
        
        <div class="table-responsive">
            <table class="table table-striped table-hover table-bordered align-middle table-results-compact">
                <thead class="table-dark">
                    <tr>
                        <th class="col-name">Name (Alter)</th>
                        <th class="col-class text-center">Klasse</th>
                        {% for category, items in disciplines %}
                            {% if category|trim|upper not in ['SWIMMING', 'SCHWIMMEN'] %}
                                <th class="text-center col-discipline">{{ category }}</th>
                            {% endif %}
                        {% endfor %}
                    </tr>
                </thead>
                <tbody>
                    {% for p in participants %}
                        {% set epResults = results[p.ep_id] ?? {} %}
                        {% set totalPoints = 0 %}
                        {# ... (Vorberechnung Punkte/Schwimmen) ... #}

                        <tr data-ep-id="{{ p.ep_id }}">
                            <td>
                                <div class="fw-bold">{{ p.vorname }} {{ p.nachname }} <span class="text-muted fw-normal">({{ p.age_year }})</span></div>
                                <div class="mt-1">
                                    <span class="badge bg-info text-dark fw-bold" id="total-points-{{ p.ep_id }}">{{ totalPoints }} Pkt.</span>
                                </div>
                            </td>
                            <td class="text-center fw-bold">{{ p.klasse }}</td>

                            {% for category, items in disciplines %}
                                {% if category|trim|upper not in ['SWIMMING', 'SCHWIMMEN'] %}
                                    {% set options = [] %}
                                    {% for d in items %}
                                        {% if d.geschlecht == p.gender and p.age_year >= d.age_min and p.age_year <= d.age_max %}
                                            {% set options = options|merge([d]) %}
                                        {% endif %}
                                    {% endfor %}

                                    {% set selectedDiscId = null %}
                                    {% set selectedValue = '' %}
                                    {% set medalClass = '' %}
                                    {% set currentReq = null %}

                                    {% for d in options %}
                                        {% if epResults[d.id] is defined %}
                                            {% set selectedDiscId = d.id %}
                                            {% set currentReq = d %}
                                            {% set selectedValue = epResults[d.id].leistung %}
                                            {% set stufe = epResults[d.id].stufe|default('')|lower %}
                                            {% if stufe in ['gold', 'silber', 'bronze'] %}
                                                {% set medalClass = 'medal-' ~ stufe %}
                                            {% endif %}
                                        {% endif %}
                                    {% endfor %}

                                    {% if selectedDiscId is null and options|length > 0 %}
                                        {% set selectedDiscId = options[0].id %}
                                        {% set currentReq = options[0] %}
                                    {% endif %}

                                    <td class="p-1 col-discipline text-center">
                                        {# Das 'name' Attribut ist oft entscheidend für den 400er Fehler #}
                                        <select name="results[{{ p.ep_id }}][{{ category }}][discipline]" 
                                                class="form-select discipline-selector mb-1 {{ medalClass }}" 
                                                data-save 
                                                data-participant="{{ p.ep_id }}" 
                                                data-category="{{ category }}">
                                            <option value="">-</option>
                                            {% for d in options %}
                                                <option value="{{ d.id }}" 
                                                        data-bronze="{{ d.bronze }}" 
                                                        data-silber="{{ d.silber }}" 
                                                        data-gold="{{ d.gold }}"
                                                        {% if selectedDiscId == d.id %}selected{% endif %}>
                                                    {{ d.name }}
                                                </option>
                                            {% endfor %}
                                        </select>
                                        
                                        <input type="text" 
                                            name="results[{{ p.ep_id }}][{{ category }}][leistung]" 
                                            class="form-control form-control-custom fw-bold {{ medalClass }}" 
                                            value="{{ selectedValue }}" 
                                            data-save
                                            data-participant="{{ p.ep_id }}" 
                                            data-category="{{ category }}">
                                        
                                        {% if currentReq %}
                                            <div class="req-hint" id="req-{{ p.ep_id }}-{{ category }}">
                                                <span class="req-bronze">{{ currentReq.bronze }}</span>
                                                <span class="req-silber">{{ currentReq.silber }}</span>
                                                <span class="req-gold">{{ currentReq.gold }}</span>
                                            </div>
                                        {% endif %}
                                    </td>
                                {% endif %}
                            {% endfor %}
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </form>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('js/exam_results_autosave.js', 'pulsr-sportabzeichen') }}"></script>
{% endblock %}